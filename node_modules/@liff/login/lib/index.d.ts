import { LiffModule, LoginHooks } from '@liff/use';
declare type Login = (config?: {
    redirectUri?: string;
}) => void;
export interface ExtendLiffCore {
    login: Login;
}
export declare type Option = void | {
    redirectUri?: string;
};
export declare class LoginModule implements LiffModule<Login, unknown, LoginHooks> {
    hooks: LoginHooks;
    get name(): string;
    install(): Login;
    /**
     * Performs the LINE Login process (web login) for the Web app.
     * @export
     */
    private login;
}
export {};
